---
phase: 01-arabic-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v2-avatar/personas/5G/quizzes.ar.js
  - src/v2-avatar/personas/5G/modules.ar.js
autonomous: true

must_haves:
  truths:
    - "Arabic quiz file exists with translations for all 6 MCQ questions and their 4 answer options each"
    - "Arabic quiz file includes translated intros, explanations, and completion messages for all 3 module quizzes"
    - "Arabic finalQuiz exists with translated conversational questions, correctAnswer, and intro/completion"
    - "Arabic modules file exists with translated section titles, module titles, descriptions, and course metadata"
    - "English quizzes.js and modules.js are completely unchanged"
  artifacts:
    - path: "src/v2-avatar/personas/5G/quizzes.ar.js"
      provides: "All Arabic quiz translations"
      exports: ["moduleQuizzes", "finalQuiz", "default"]
    - path: "src/v2-avatar/personas/5G/modules.ar.js"
      provides: "Arabic module metadata translations"
      exports: ["courseStructure", "moduleDefinitions", "moduleOrder", "modulesRequiringConfirmation", "sectionEndingModules", "courseMeta", "default"]
  key_links:
    - from: "src/v2-avatar/personas/5G/quizzes.ar.js"
      to: "src/v2-avatar/personas/5G/quizzes.js"
      via: "identical export structure, quiz keys, question count, correctIndex values"
      pattern: "export const moduleQuizzes"
    - from: "src/v2-avatar/personas/5G/modules.ar.js"
      to: "src/v2-avatar/personas/5G/modules.js"
      via: "identical export structure, module IDs, order arrays, feature flags"
      pattern: "export const moduleDefinitions"
---

<objective>
Create Arabic translations for all 5G course structured content: quiz questions/answers and module metadata.

Purpose: These files provide the Arabic quiz content (questions, options, explanations, messages) and module display text (titles, descriptions, section names) for the 5G course when Arabic is selected.

Output: Two new files (`quizzes.ar.js`, `modules.ar.js`) that mirror the English originals in structure but contain Arabic translations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/v2-avatar/personas/5G/quizzes.js
@src/v2-avatar/personas/5G/modules.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Arabic quizzes file (quizzes.ar.js)</name>
  <files>src/v2-avatar/personas/5G/quizzes.ar.js</files>
  <action>
Create `src/v2-avatar/personas/5G/quizzes.ar.js` that is a structural mirror of `quizzes.js` with all text translated to Modern Standard Arabic (MSA).

Read `quizzes.js` first. Then create `quizzes.ar.js` with:

1. **Same file header comment** (English)
2. **`moduleQuizzes` object** with identical keys: `quiz-5g-basics`, `quiz-technical-specs`, `final-quiz`. For each quiz:
   - `intro`: Arabic translation
   - `questions` array with SAME number of questions, SAME `correctIndex` values:
     - `question`: Arabic translation
     - `options`: Array of 4 Arabic translations (SAME order -- correctIndex must still point to the correct answer)
     - `correctIndex`: IDENTICAL to English (do NOT change these numbers)
     - `explanation`: Arabic translation
   - `passingScore`: IDENTICAL to English
   - `completionMessage`: Arabic translation

3. **`finalQuiz` object** with:
   - `intro`: Arabic translation
   - `questions` array with SAME structure:
     - `question`: Arabic translation
     - `correctAnswer`: Keep in English (these are technical values like "eMBB, URLLC, mMTC" and "20 Gbps" that users type)
     - `keywords`: Keep in English (these are matching tokens, not displayed)
     - `topic`: Arabic translation
   - `passingScore`: IDENTICAL to English
   - `completionMessage`: Arabic translation

4. **Default export** `fiveGQuizzes` with same shape.

CRITICAL: `correctIndex` values and `passingScore` values MUST be identical to English. These are logic values, not content. Getting these wrong breaks quiz scoring.

CRITICAL: `keywords` and `correctAnswer` in finalQuiz stay English -- these are internal matching values.

Translation guidelines:
- Modern Standard Arabic, educational tone
- Technical terms stay English: 5G, 4G, eMBB, URLLC, mMTC, Wi-Fi, mmWave, 3GPP, LTE, Mbps, Gbps
- Answer options must be translated but maintain the same semantic meaning and order
- Quiz instructions about clicking on screen should be in Arabic

Do NOT modify `quizzes.js`.
  </action>
  <verify>
1. File exists: `ls src/v2-avatar/personas/5G/quizzes.ar.js`
2. Has all 3 moduleQuizzes keys: quiz-5g-basics, quiz-technical-specs, final-quiz
3. Each quiz has correct number of questions (2 each = 6 total)
4. correctIndex values match English: quiz-5g-basics [1, 3], quiz-technical-specs [2, 1], final-quiz [1, 1]
5. Has finalQuiz with 2 questions, keywords in English
6. Contains Arabic characters
7. Valid JS syntax: `node -c src/v2-avatar/personas/5G/quizzes.ar.js` (syntax check)
8. English file unchanged: `git diff src/v2-avatar/personas/5G/quizzes.js` shows no changes
  </verify>
  <done>
- quizzes.ar.js exists with all 6 MCQ questions translated (2 per quiz x 3 quizzes)
- All 24 answer options translated (4 per question x 6 questions)
- All correctIndex values identical to English version
- All passingScore values identical to English version
- finalQuiz keywords and correctAnswer kept in English
- 3 quiz intros, 3 completion messages, 6 explanations all translated
- Export structure matches quizzes.js exactly
- English quizzes.js has zero modifications
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Arabic modules file (modules.ar.js)</name>
  <files>src/v2-avatar/personas/5G/modules.ar.js</files>
  <action>
Create `src/v2-avatar/personas/5G/modules.ar.js` that mirrors `modules.js` structurally with Arabic translations for user-visible text.

Read `modules.js` first. Then create `modules.ar.js` with:

1. **Same file header comment** (English)
2. **Same ICONS constant** (identical -- these are icon names, not translatable)
3. **`courseStructure` array** with identical structure but Arabic `title` values:
   - "1. Introduction to 5G" -> Arabic translation (keep "5G" in English)
   - "2. Speed and Latency" -> Arabic translation
   - "3. Real-World Applications" -> Arabic translation
   - `id` and `items` arrays stay IDENTICAL to English

4. **`moduleDefinitions` object** with identical keys and structure. For each module:
   - `id`: IDENTICAL to English (these are programmatic IDs)
   - `title`: Arabic translation
   - `description`: Arabic translation
   - `type`: IDENTICAL (programmatic)
   - `duration`: IDENTICAL
   - `icon`: IDENTICAL
   - `hasVideo`: IDENTICAL
   - `videoUrl`: IDENTICAL (if present)
   - `hasPresentation`: IDENTICAL (if present)
   - `presentationConfig`: IDENTICAL (if present)
   - `questions`: IDENTICAL (if present)

5. **`moduleOrder` array** -- IDENTICAL to English (these are IDs)
6. **`modulesRequiringConfirmation` array** -- IDENTICAL to English (IDs)
7. **`sectionEndingModules` array** -- IDENTICAL to English (IDs)
8. **`courseMeta` object**:
   - `title`: Arabic translation
   - `description`: Arabic translation

9. **Default export** `fiveGModules` with same shape.

CRITICAL: Only translate `title` and `description` fields that are user-visible text. All IDs, types, URLs, durations, booleans, and arrays of IDs must be IDENTICAL to English.

Do NOT modify `modules.js`.
  </action>
  <verify>
1. File exists: `ls src/v2-avatar/personas/5G/modules.ar.js`
2. Has all 7 moduleDefinitions keys matching English
3. moduleOrder array identical to English
4. All module IDs unchanged
5. courseStructure has 3 sections with Arabic titles
6. courseMeta has Arabic title and description
7. Contains Arabic characters
8. Valid JS syntax: `node -c src/v2-avatar/personas/5G/modules.ar.js` (syntax check)
9. English file unchanged: `git diff src/v2-avatar/personas/5G/modules.js` shows no changes
  </verify>
  <done>
- modules.ar.js exists with Arabic titles and descriptions for all 7 modules
- 3 courseStructure section titles translated
- courseMeta title and description translated
- All IDs, order arrays, feature flags, URLs, durations identical to English
- Export structure matches modules.js exactly
- English modules.js has zero modifications
  </done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:

```bash
# 1. Both files exist
ls -la src/v2-avatar/personas/5G/quizzes.ar.js
ls -la src/v2-avatar/personas/5G/modules.ar.js

# 2. Arabic content present
grep -P '[\x{0600}-\x{06FF}]' src/v2-avatar/personas/5G/quizzes.ar.js | head -5
grep -P '[\x{0600}-\x{06FF}]' src/v2-avatar/personas/5G/modules.ar.js | head -5

# 3. Quiz keys present
for key in quiz-5g-basics quiz-technical-specs final-quiz; do
  grep -q "'$key'" src/v2-avatar/personas/5G/quizzes.ar.js && echo "OK: $key" || echo "MISSING: $key"
done

# 4. Module keys present
for key in intro-5g 5g-revolution-video quiz-5g-basics speed-latency-pdf quiz-technical-specs real-world-applications final-quiz; do
  grep -q "'$key'" src/v2-avatar/personas/5G/modules.ar.js && echo "OK: $key" || echo "MISSING: $key"
done

# 5. correctIndex values preserved (spot check)
grep "correctIndex" src/v2-avatar/personas/5G/quizzes.ar.js

# 6. Module IDs preserved (no Arabic in IDs)
grep "id:" src/v2-avatar/personas/5G/modules.ar.js

# 7. English files untouched
git diff src/v2-avatar/personas/5G/quizzes.js
git diff src/v2-avatar/personas/5G/modules.js

# 8. Syntax check
node -c src/v2-avatar/personas/5G/quizzes.ar.js
node -c src/v2-avatar/personas/5G/modules.ar.js
```
</verification>

<success_criteria>
- Two new Arabic files created covering ARBC-02 (quiz questions and answers) and module metadata
- All 6 MCQ questions with 24 answer options translated
- correctIndex and passingScore values preserved exactly from English
- finalQuiz keywords and correctAnswer kept in English (internal matching)
- Module titles, descriptions, section titles, and course metadata translated
- All programmatic values (IDs, types, URLs, booleans) identical to English
- Export structures mirror English originals exactly
- English source files have zero modifications
- Files are valid JavaScript modules
</success_criteria>

<output>
After completion, create `.planning/phases/01-arabic-content/01-02-SUMMARY.md`
</output>
