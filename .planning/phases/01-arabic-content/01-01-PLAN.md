---
phase: 01-arabic-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v2-avatar/personas/5G/prompts.ar.js
  - src/v2-avatar/personas/5G/presentations/speed-latency.ar.js
autonomous: true

must_haves:
  truths:
    - "Arabic prompts file exists with translations for all 7 modulePrompts entries"
    - "Arabic moduleTransitionPrompts exist for both section-ending modules"
    - "Arabic videoCompletionPrompts, courseCompletionPrompt, and welcomeMessage exist"
    - "Arabic PDF narration exists for speed-latency presentation"
    - "All Arabic text uses natural Modern Standard Arabic appropriate for educational content"
    - "English prompts.js and presentations/speed-latency.js are completely unchanged"
  artifacts:
    - path: "src/v2-avatar/personas/5G/prompts.ar.js"
      provides: "All Arabic prompt translations"
      exports: ["modulePrompts", "moduleTransitionPrompts", "videoCompletionPrompts", "courseCompletionPrompt", "welcomeMessage", "default"]
    - path: "src/v2-avatar/personas/5G/presentations/speed-latency.ar.js"
      provides: "Arabic narration for speed-latency PDF"
      exports: ["speedLatencyPresentation", "default"]
  key_links:
    - from: "src/v2-avatar/personas/5G/prompts.ar.js"
      to: "src/v2-avatar/personas/5G/prompts.js"
      via: "identical export structure and object keys"
      pattern: "export const modulePrompts"
    - from: "src/v2-avatar/personas/5G/presentations/speed-latency.ar.js"
      to: "src/v2-avatar/personas/5G/presentations/speed-latency.js"
      via: "identical export structure, same pdfUrl, Arabic narration"
      pattern: "export const speedLatencyPresentation"
---

<objective>
Create Arabic translations for all 5G course spoken content: avatar prompts and PDF narration.

Purpose: These files provide the Arabic text the avatar will speak during the 5G course when Arabic language is selected. They cover module introductions, transitions, video completion messages, course completion, welcome message, and PDF slide narrations.

Output: Two new files (`prompts.ar.js`, `presentations/speed-latency.ar.js`) that mirror the English originals in structure but contain Arabic translations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/v2-avatar/personas/5G/prompts.js
@src/v2-avatar/personas/5G/presentations/speed-latency.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Arabic prompts file (prompts.ar.js)</name>
  <files>src/v2-avatar/personas/5G/prompts.ar.js</files>
  <action>
Create `src/v2-avatar/personas/5G/prompts.ar.js` that is a structural mirror of `prompts.js` with all text translated to Modern Standard Arabic (MSA).

Read `prompts.js` first. Then create `prompts.ar.js` with:

1. **Same file header comment** (in English -- code comments stay English for maintainability)
2. **`modulePrompts` object** with identical keys: `intro-5g`, `5g-revolution-video`, `quiz-5g-basics`, `speed-latency-pdf`, `quiz-technical-specs`, `real-world-applications`, `final-quiz`. Each value is the Arabic translation of the corresponding English prompt.
3. **`moduleTransitionPrompts` object** with identical keys: `quiz-5g-basics`, `quiz-technical-specs`. Arabic translations.
4. **`videoCompletionPrompts` object** with key `5g-revolution-video`. Arabic translation.
5. **`courseCompletionPrompt` string** -- Arabic translation.
6. **`welcomeMessage` string** -- Arabic translation.
7. **Default export** `fiveGPrompts` object with same shape as English version.

Translation guidelines:
- Use Modern Standard Arabic (MSA) -- formal but accessible educational tone
- Keep technical terms in their English form where standard in Arabic tech education: 5G, 4G, eMBB, URLLC, mMTC, Wi-Fi, IoT, mmWave, 3GPP. These are universally used in Arabic telecom contexts.
- Translate the descriptive expansions of acronyms (e.g., "Enhanced Mobile Broadband" gets an Arabic translation alongside the English acronym)
- Preserve ellipses for TTS pacing pauses (the `   ` pattern used in the English)
- Preserve blank line spacing between paragraphs (important for TTS breathing)
- The avatar name "Ann" stays as "Ann" (it's the avatar's name)
- Use Arabic quotation marks where appropriate
- "say continue" becomes the Arabic equivalent instruction
- Keep the same approximate length/density per prompt -- do not over-expand or truncate

Do NOT modify `prompts.js`. The English file must remain completely unchanged.
  </action>
  <verify>
1. File exists: `ls src/v2-avatar/personas/5G/prompts.ar.js`
2. Has all 7 modulePrompts keys: grep for each key name (intro-5g, 5g-revolution-video, quiz-5g-basics, speed-latency-pdf, quiz-technical-specs, real-world-applications, final-quiz)
3. Has both moduleTransitionPrompts keys (quiz-5g-basics, quiz-technical-specs)
4. Has videoCompletionPrompts with key 5g-revolution-video
5. Has courseCompletionPrompt export
6. Has welcomeMessage export
7. Has default export
8. Contains Arabic characters (grep for Arabic Unicode range)
9. English file unchanged: `git diff src/v2-avatar/personas/5G/prompts.js` shows no changes
  </verify>
  <done>
- prompts.ar.js exists with all 12 prompt entries translated to Arabic (7 modulePrompts + 2 moduleTransitionPrompts + 1 videoCompletionPrompt + 1 courseCompletionPrompt + 1 welcomeMessage)
- Export structure matches prompts.js exactly (same named exports, same default export shape)
- All object keys are identical to English version
- Text is Modern Standard Arabic with technical terms preserved in English
- English prompts.js has zero modifications
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Arabic PDF narration file (speed-latency.ar.js)</name>
  <files>src/v2-avatar/personas/5G/presentations/speed-latency.ar.js</files>
  <action>
Create `src/v2-avatar/personas/5G/presentations/speed-latency.ar.js` that mirrors `speed-latency.js` structurally.

Read `presentations/speed-latency.js` first. Then create `speed-latency.ar.js` with:

1. **Same file header comment** (English)
2. **`speedLatencyPresentation` object** with:
   - `pdfUrl`: SAME URL as English version (`https://storage.googleapis.com/qudemo-test-qudemo/5G.pdf`) -- the PDF stays English per ARBC-03
   - `slides` array with one entry:
     - `page: 1` (same)
     - `title: 'Speed and Latency'` -- translate to Arabic
     - `narration`: Arabic translation of the narration text
3. **Default export** `speedLatencyPresentation`

Translation guidelines: Same as Task 1 (MSA, preserve ellipses for TTS pacing, keep technical terms in English).

Do NOT modify `presentations/speed-latency.js`.
  </action>
  <verify>
1. File exists: `ls src/v2-avatar/personas/5G/presentations/speed-latency.ar.js`
2. Has speedLatencyPresentation export
3. pdfUrl matches English version exactly
4. Has page: 1 slide with Arabic narration
5. Contains Arabic characters
6. English file unchanged: `git diff src/v2-avatar/personas/5G/presentations/speed-latency.js` shows no changes
  </verify>
  <done>
- speed-latency.ar.js exists with Arabic narration for the PDF presentation
- pdfUrl is identical to English version (PDF stays English)
- Slide structure matches English (same page numbers)
- Narration text is Modern Standard Arabic
- English speed-latency.js has zero modifications
  </done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:

```bash
# 1. Both files exist
ls -la src/v2-avatar/personas/5G/prompts.ar.js
ls -la src/v2-avatar/personas/5G/presentations/speed-latency.ar.js

# 2. Arabic content present (check for Arabic Unicode characters)
grep -P '[\x{0600}-\x{06FF}]' src/v2-avatar/personas/5G/prompts.ar.js | head -5
grep -P '[\x{0600}-\x{06FF}]' src/v2-avatar/personas/5G/presentations/speed-latency.ar.js | head -5

# 3. All modulePrompts keys present
for key in intro-5g 5g-revolution-video quiz-5g-basics speed-latency-pdf quiz-technical-specs real-world-applications final-quiz; do
  grep -q "'$key'" src/v2-avatar/personas/5G/prompts.ar.js && echo "OK: $key" || echo "MISSING: $key"
done

# 4. English files untouched
git diff src/v2-avatar/personas/5G/prompts.js
git diff src/v2-avatar/personas/5G/presentations/speed-latency.js
```
</verification>

<success_criteria>
- Two new Arabic files created covering ARBC-01 (module prompts), ARBC-03 (PDF narration), ARBC-04 (course completion), ARBC-05 (module transitions)
- Export structures mirror English originals exactly
- All text is proper Modern Standard Arabic
- English source files have zero modifications
- Files are importable JavaScript modules (valid syntax)
</success_criteria>

<output>
After completion, create `.planning/phases/01-arabic-content/01-01-SUMMARY.md`
</output>
